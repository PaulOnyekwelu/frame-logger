{
  "name": "frame-logger",
  "version": "2.0.0",
  "description": "Lightweight framing utilities for console output (multi-line, configurable styles).",
  "main": "index.js",
  "files": [
    "index.js",
    "src"
  ],
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "test:update": "cross-env UPDATE_SNAPSHOTS=1 node --test"
  },
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
